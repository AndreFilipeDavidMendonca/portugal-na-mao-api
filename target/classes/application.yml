server:
  port: ${PORT:8085}
  forward-headers-strategy: framework

  tomcat:
    max-swallow-size: 20MB

  servlet:
    session:
      tracking-modes: cookie

  compression:
    enabled: true
    min-response-size: 1024
    mime-types: application/json,text/plain,text/html,text/xml,text/css,application/javascript

spring:
  application:
    name: monumentos-proxy

  main:
    web-application-type: servlet

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 120MB

  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5432/pt_dot}
    username: ${JDBC_DATABASE_USERNAME:ptdot_user}
    password: ${JDBC_DATABASE_PASSWORD:ptdot_pass}
    hikari:
      maximum-pool-size: ${DB_POOL_MAX:8}
      minimum-idle: ${DB_POOL_MIN_IDLE:1}
      connection-timeout: ${DB_CONN_TIMEOUT_MS:30000}
      validation-timeout: ${DB_VALIDATION_TIMEOUT_MS:5000}
      idle-timeout: ${DB_IDLE_TIMEOUT_MS:600000}
      max-lifetime: ${DB_MAX_LIFETIME_MS:1800000}

  jpa:
    open-in-view: false
    show-sql: ${SHOW_SQL:false}
    hibernate:
      ddl-auto: ${HIBERNATE_DDL_AUTO:validate}
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never

  cache:
    type: caffeine

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: ${FLYWAY_BASELINE_ON_MIGRATE:true}
    baseline-version: ${FLYWAY_BASELINE_VERSION:1}
    baseline-description: ${FLYWAY_BASELINE_DESCRIPTION:baseline}
    validate-on-migrate: true
    out-of-order: ${FLYWAY_OUT_OF_ORDER:false}

app:
  geocoding:
    nominatim:
      base-url: ${NOMINATIM_BASE_URL:https://nominatim.openstreetmap.org}
      user-agent: "${NOMINATIM_USER_AGENT:pt-dot-app/1.0 (contact: dev@pt-dot.local)}"
    cache:
      ttl-seconds: 604800

ptdot:
  jwt:
    secret: ${PTDOT_JWT_SECRET:CHANGE_ME_TO_A_LONG_RANDOM_SECRET_32+_CHARS}
    issuer: ${PTDOT_JWT_ISSUER:ptdot}
    accessTokenMinutes: ${PTDOT_JWT_ACCESS_MINUTES:60}

  media:
    base-url: ${PTDOT_MEDIA_BASE_URL:https://pub-f401bb0bb80c4136bf61b8d4c5e88925.r2.dev}
    r2:
      enabled: ${PTDOT_MEDIA_R2_ENABLED:false}
      bucket: ${PTDOT_MEDIA_R2_BUCKET:ptdot-media-dev}
      account-id: ${PTDOT_MEDIA_R2_ACCOUNT_ID:}
      endpoint: ${PTDOT_MEDIA_R2_ENDPOINT:}
      access-key-id: ${PTDOT_MEDIA_R2_ACCESS_KEY_ID:}
      secret-access-key: ${PTDOT_MEDIA_R2_SECRET_ACCESS_KEY:}
      region: ${PTDOT_MEDIA_R2_REGION:auto}

  bootstrap:
    reconcile-schema: ${PTDOT_RECONCILE_SCHEMA:false}
    schema-script-path: ${PTDOT_SCHEMA_SCRIPT_PATH:classpath:/db/bootstrap/schema_pt_dot.sql}
    populate: ${PTDOT_POPULATE:false}
    populate-script-path: ${PTDOT_POPULATE_SCRIPT_PATH:classpath:/db/bootstrap/populate_pt_dot.sql}
    populate-if-empty: ${PTDOT_POPULATE_IF_EMPTY:true}
    populate-districts: ${PTDOT_POPULATE_DISTRICTS:true}
    populate-district-files: ${PTDOT_POPULATE_DISTRICT_FILES:true}
    populate-pois: ${PTDOT_POPULATE_POIS:true}
    reset-pois: ${PTDOT_RESET_POIS:false}
    import-districts: ${PTDOT_IMPORT_DISTRICTS:false}
    import-district-files: ${PTDOT_IMPORT_DISTRICT_FILES:false}
    sync-pois-from-csv: ${PTDOT_SYNC_POIS_FROM_CSV:false}

logging:
  level:
    pt.dot.application.service: ${LOG_LEVEL_SERVICE:INFO}
    pt.dot.application.monumentos: ${LOG_LEVEL_MONUMENTOS:INFO}

---
spring:
  config:
    activate:
      on-profile: local

  jpa:
    show-sql: ${SHOW_SQL:true}
    hibernate:
      ddl-auto: ${HIBERNATE_DDL_AUTO:validate}

ptdot:
  media:
    base-url: ${PTDOT_MEDIA_BASE_URL:https://pub-f401bb0bb80c4136bf61b8d4c5e88925.r2.dev}
    r2:
      enabled: ${PTDOT_MEDIA_R2_ENABLED:true}
      bucket: ${PTDOT_MEDIA_R2_BUCKET:ptdot-media-dev}
      account-id: ${PTDOT_MEDIA_R2_ACCOUNT_ID:8ff825c7f2fd37cf0e32c6984f82620f}
      endpoint: ${PTDOT_MEDIA_R2_ENDPOINT:https://8ff825c7f2fd37cf0e32c6984f82620f.r2.cloudflarestorage.com}
      region: ${PTDOT_MEDIA_R2_REGION:auto}

---
spring:
  config:
    activate:
      on-profile: local

  jpa:
    show-sql: ${SHOW_SQL:true}
    hibernate:
      ddl-auto: ${HIBERNATE_DDL_AUTO:validate}

  flyway:
    baseline-on-migrate: ${FLYWAY_BASELINE_ON_MIGRATE:true}

ptdot:
  media:
    base-url: ${PTDOT_MEDIA_BASE_URL:https://pub-f401bb0bb80c4136bf61b8d4c5e88925.r2.dev}
    r2:
      enabled: ${PTDOT_MEDIA_R2_ENABLED:true}
      bucket: ${PTDOT_MEDIA_R2_BUCKET:ptdot-media-dev}
      account-id: ${PTDOT_MEDIA_R2_ACCOUNT_ID:8ff825c7f2fd37cf0e32c6984f82620f}
      endpoint: ${PTDOT_MEDIA_R2_ENDPOINT:https://8ff825c7f2fd37cf0e32c6984f82620f.r2.cloudflarestorage.com}
      access-key-id: ${PTDOT_MEDIA_R2_ACCESS_KEY_ID:}
      secret-access-key: ${PTDOT_MEDIA_R2_SECRET_ACCESS_KEY:}
      region: ${PTDOT_MEDIA_R2_REGION:auto}

---
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: ${JDBC_DATABASE_URL}
    username: ${JDBC_DATABASE_USERNAME}
    password: ${JDBC_DATABASE_PASSWORD}
    hikari:
      maximum-pool-size: ${DB_POOL_MAX:10}

  jpa:
    show-sql: ${SHOW_SQL:false}
    hibernate:
      ddl-auto: ${HIBERNATE_DDL_AUTO:validate}

  flyway:
    enabled: true
    baseline-on-migrate: ${FLYWAY_BASELINE_ON_MIGRATE:true}

ptdot:
  media:
    base-url: ${PTDOT_MEDIA_BASE_URL}
    r2:
      enabled: ${PTDOT_MEDIA_R2_ENABLED:true}
      bucket: ${PTDOT_MEDIA_R2_BUCKET}
      account-id: ${PTDOT_MEDIA_R2_ACCOUNT_ID}
      endpoint: ${PTDOT_MEDIA_R2_ENDPOINT}
      access-key-id: ${PTDOT_MEDIA_R2_ACCESS_KEY_ID}
      secret-access-key: ${PTDOT_MEDIA_R2_SECRET_ACCESS_KEY}
      region: ${PTDOT_MEDIA_R2_REGION:auto}

---
spring:
  config:
    activate:
      on-profile: baseline

  flyway:
    locations: classpath:db/baseline
    baseline-on-migrate: false
    validate-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: validate