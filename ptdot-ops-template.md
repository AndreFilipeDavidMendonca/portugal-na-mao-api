# .pt - OPERATIONS TEMPLATE
# Seguro para partilhar com equipa (sem secrets)

## Backend
BACKEND_URL="<BACKEND_RENDER_URL>"

## PostgreSQL (Render PROD)
RENDER_PG_HOST="<RENDER_PG_HOST>"
RENDER_PG_PORT="5432"
RENDER_PG_DB="pt_dot"
RENDER_PG_USER="<RENDER_DB_USER>"
RENDER_PG_PASSWORD="<RENDER_DB_PASSWORD>"

## PostgreSQL (Local)
LOCAL_PG_HOST="localhost"
LOCAL_PG_PORT="5432"
LOCAL_PG_DB="pt_dot"
LOCAL_PG_USER="ptdot_user"
LOCAL_PG_PASSWORD="<LOCAL_DB_PASSWORD>"

## R2 (Cloudflare)
R2_PUBLIC_BASE_URL="<R2_PUBLIC_BASE_URL>"
R2_ACCOUNT_ID="<R2_ACCOUNT_ID>"
R2_ENDPOINT="<R2_ENDPOINT>"
R2_BUCKET="<R2_BUCKET>"
R2_ACCESS_KEY_ID="<R2_ACCESS_KEY_ID>"
R2_SECRET_ACCESS_KEY="<R2_SECRET_ACCESS_KEY>"

## JWT
PTDOT_JWT_SECRET="<JWT_SECRET>"
PTDOT_JWT_ISSUER="ptdot"
PTDOT_JWT_ACCESS_MINUTES="60"

---

## Comandos úteis

### Ligar à BD PROD
psql -h "$RENDER_PG_HOST" -p "$RENDER_PG_PORT" -U "$RENDER_PG_USER" -d "$RENDER_PG_DB"

### Health check
curl "$BACKEND_URL/actuator/health"

### Ver POI
curl "$BACKEND_URL/api/pois/2"